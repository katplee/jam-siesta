using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CustomerMovementNode : MonoBehaviour
{
    /*
    // Start is called before the first frame update
    void Start()
    {   
        //ADD NODE TO CUSTOMER POSITION DICTIONARY
        //POSITION IS SENT IN TERMS OF CELL POSITION
        Vector3Int nodeTilePos = Tilemaps.Instance.customerTileMap.WorldToCell(transform.position);
        GameManager.Instance.customerPosition.Add(gameObject.name, nodeTilePos);
    } 
    */
}
